<!-- QAgentic. A Q, because the work is quality; the tick inside it, because the
     only claim this platform makes is one it verified; the tail, because a Q
     without one is just a circle and every other tool's icon is a circle. -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
  <circle cx="10.5" cy="10.5" r="7.2"/>
  <path d="M15.8 15.8 20 20"/>
  <path d="M7 10.8 9.6 13.4 14 8.2"/>
</svg>
